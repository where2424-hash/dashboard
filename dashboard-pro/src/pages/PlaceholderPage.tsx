export function PlaceholderPage({ title }: { title: string }) {
  return (
    <section style={{ padding: "40px", textAlign: "center", color: "#888", fontSize: "12px" }}>
      {title}
    </section>
  );
}
